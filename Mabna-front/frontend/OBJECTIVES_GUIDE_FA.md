# راهنمای صفحه اهداف جدید

## 🎯 خلاصه

صفحه اهداف با یک طراحی مدرن و زیبا بازطراحی شده است که شامل:
- کارت‌های تعاملی با انیمیشن
- حلقه‌های پیشرفت بصری
- نمایش Grid سه ستونی
- پشتیبانی کامل از حالت تاریک

## 📁 فایل‌های ایجاد شده

```
frontend/src/
├── components/
│   ├── ui/
│   │   └── activity-card.jsx          # کامپوننت اصلی کارت
│   ├── examples/
│   │   └── activity-card-demo.jsx     # نمونه استفاده
│   ├── ObjectivesModern.jsx           # صفحه اهداف جدید
│   └── ObjectivesModern.css           # استایل‌های سفارشی
```

## 🚀 نحوه استفاده

### 1. فعال‌سازی در Dashboard

کامپوننت جدید به صورت خودکار در Dashboard فعال شده است:

```jsx
// در Dashboard.jsx
import ObjectivesModern from './ObjectivesModern';

{activeTab === 'objectives' && <ObjectivesModern token={token} />}
```

### 2. تست کامپوننت

برای تست کامپوننت می‌توانید از فایل دمو استفاده کنید:

```jsx
import { ActivityCardDemo } from './components/examples/activity-card-demo';

// در App.jsx یا هر جای دیگر
<ActivityCardDemo />
```

## 🎨 ویژگی‌های کارت فعالیت

### متریک‌ها (Metrics)
هر کارت شامل 3 متریک است:
1. **نتایج کلیدی**: تعداد کل نتایج کلیدی
2. **تکمیل شده**: تعداد نتایج کلیدی تکمیل شده
3. **پیشرفت**: درصد میانگین پیشرفت

### حلقه‌های پیشرفت
- نمایش بصری درصد پیشرفت
- انیمیشن hover
- رنگ‌های متفاوت برای هر متریک

### اهداف روزانه
- لیست نتایج کلیدی
- امکان toggle کردن وضعیت
- نمایش خط خورده برای موارد تکمیل شده

### دکمه‌های عملیاتی
- **+**: افزودن نتیجه کلیدی جدید
- **View Details**: مشاهده گزارش کامل

## 📊 ساختار داده

### ورودی ActivityCard

```javascript
{
  category: "1402/01/01 - 1402/03/31",  // تاریخ شروع و پایان
  title: "رشد کسب‌وکار",                // عنوان هدف
  metrics: [
    {
      label: "نتایج کلیدی",
      value: "5",
      trend: 85,                         // درصد پیشرفت (0-100)
      unit: "عدد"
    },
    // ...
  ],
  dailyGoals: [
    {
      id: "1",
      title: "افزایش فروش 20%",
      isCompleted: true
    },
    // ...
  ],
  onAddGoal: () => {},                   // تابع افزودن هدف
  onToggleGoal: (goalId) => {},          // تابع toggle هدف
  onViewDetails: () => {}                // تابع مشاهده جزئیات
}
```

## 🎯 مثال کامل

```jsx
import { ActivityCard } from '@/components/ui/activity-card';

function MyObjectives() {
  const handleAddGoal = () => {
    console.log('افزودن نتیجه کلیدی جدید');
  };

  const handleToggleGoal = (goalId) => {
    console.log('Toggle goal:', goalId);
  };

  const handleViewDetails = () => {
    console.log('مشاهده جزئیات');
  };

  return (
    <ActivityCard
      category="Q1 2024"
      title="رشد کسب‌وکار"
      metrics={[
        { label: "نتایج کلیدی", value: "5", trend: 85, unit: "عدد" },
        { label: "تکمیل شده", value: "3", trend: 60, unit: "عدد" },
        { label: "پیشرفت", value: "75", trend: 75, unit: "%" }
      ]}
      dailyGoals={[
        { id: "1", title: "افزایش فروش 20%", isCompleted: true },
        { id: "2", title: "کاهش هزینه‌ها 15%", isCompleted: false }
      ]}
      onAddGoal={handleAddGoal}
      onToggleGoal={handleToggleGoal}
      onViewDetails={handleViewDetails}
    />
  );
}
```

## 🎨 سفارشی‌سازی

### تغییر رنگ حلقه‌ها

در فایل `activity-card.jsx`:

```javascript
const METRIC_COLORS = {
  Move: "#FF2D55",      // قرمز
  Exercise: "#2CD758",  // سبز
  Stand: "#007AFF",     // آبی
};
```

### تغییر تعداد ستون‌ها

در فایل `ObjectivesModern.jsx`:

```jsx
// برای 2 ستون در دسکتاپ:
<div className="grid grid-cols-1 md:grid-cols-2 gap-6">

// برای 4 ستون در دسکتاپ:
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
```

### تغییر رنگ پس‌زمینه

```jsx
<div className="min-h-screen bg-gradient-to-br from-blue-50 to-purple-100 p-8">
```

## 🌙 حالت تاریک

کامپوننت به صورت خودکار از حالت تاریک پشتیبانی می‌کند:

```jsx
// کلاس‌های Tailwind با پیشوند dark:
className="bg-white dark:bg-black/5"
```

## 📱 Responsive

کامپوننت در تمام سایزها به خوبی کار می‌کند:
- **موبایل**: 1 ستون
- **تبلت**: 2 ستون
- **دسکتاپ**: 3 ستون

## 🔧 عیب‌یابی

### کامپوننت نمایش داده نمی‌شود

1. مطمئن شوید که `lucide-react` نصب شده:
```bash
npm install lucide-react
```

2. بررسی کنید که `@/lib/utils` وجود دارد:
```javascript
// src/lib/utils.js
import { clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs) {
  return twMerge(clsx(inputs));
}
```

3. مطمئن شوید که Tailwind به درستی کانفیگ شده

### استایل‌ها اعمال نمی‌شوند

1. بررسی کنید که `ObjectivesModern.css` import شده
2. مطمئن شوید که Tailwind CSS فعال است
3. کش مرورگر را پاک کنید

### انیمیشن‌ها کار نمی‌کنند

1. بررسی کنید که `tailwindcss-animate` نصب شده:
```bash
npm install tailwindcss-animate
```

2. در `tailwind.config.js` اضافه کنید:
```javascript
plugins: [require("tailwindcss-animate")]
```

## 🔄 بازگشت به نسخه قبلی

اگر می‌خواهید به نسخه قبلی برگردید:

```jsx
// در Dashboard.jsx
import Objectives from './Objectives';

{activeTab === 'objectives' && <Objectives token={token} />}
```

## 📝 نکات مهم

1. **RTL Support**: تمام استایل‌ها با راست‌چین فارسی سازگار هستند
2. **Performance**: کامپوننت بهینه شده و سریع است
3. **Accessibility**: از استانداردهای دسترسی‌پذیری پیروی می‌کند
4. **Type Safety**: می‌توانید TypeScript اضافه کنید

## 🚀 بهبودهای آینده

- [ ] افزودن drag & drop برای مرتب‌سازی
- [ ] نمایش نمودار پیشرفت
- [ ] افزودن فیلتر و جستجو
- [ ] Export به PDF/Excel
- [ ] اضافه کردن تاریخچه تغییرات
- [ ] نوتیفیکیشن برای deadline‌ها

## 💡 نکات کاربردی

### استفاده از متریک‌های سفارشی

```javascript
const customMetrics = [
  { label: "فروش", value: "120M", trend: 95, unit: "تومان" },
  { label: "مشتری", value: "450", trend: 80, unit: "نفر" },
  { label: "رضایت", value: "4.8", trend: 96, unit: "⭐" }
];
```

### اضافه کردن آیکون سفارشی

```jsx
import { Rocket } from 'lucide-react';

<div className="p-2 rounded-full bg-zinc-100">
  <Rocket className="w-5 h-5 text-[#FF2D55]" />
</div>
```

### استفاده از رنگ‌های سفارشی

```javascript
const CUSTOM_COLORS = {
  Sales: "#10b981",     // سبز
  Marketing: "#f59e0b", // نارنجی
  Product: "#8b5cf6",   // بنفش
};
```

## 📞 پشتیبانی

در صورت بروز مشکل:
1. لاگ‌های console را بررسی کنید
2. نسخه وابستگی‌ها را چک کنید
3. به مستندات shadcn مراجعه کنید
4. از کامپوننت دمو برای تست استفاده کنید

---

**نکته**: این کامپوننت با الهام از Apple Activity Rings طراحی شده است 🍎
